// Import the default Prettier config file and expose it in the project root.
// Useful for editor integrations.

// Note: The `module.exports` statement is not necessary in Node.js v18 and later,
// as the default export is automatically exposed. However, we are including it
// for compatibility with older versions of Node.js.

module.exports = require('@wordpress/prettier-config');
